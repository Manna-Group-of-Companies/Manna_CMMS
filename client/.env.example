# Copy to .env and adjust. Only VITE_-prefixed vars reach the browser.
# Never put secrets here - the bundle is public.

# Backend API base URL, including the /api suffix, no trailing slash.
VITE_API_URL=https://manna-cmms.onrender.com/api
